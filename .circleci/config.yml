version: 2.1

orbs:
  docker: circleci/docker@2.0

jobs:
  build:
    machine:
      image: ubuntu-2204:current
    steps:
      - checkout
      - run:
          name: Build Docker image
          command: docker build -t node-js-server .
      - run:
          name: list image
          command: docker image list
      - run:
          name: Run Docker container
          command: docker run -d -p 8080:8080 --name node-js-server-container node-js-server
      - run:
          name: Wait for the server to start
          command: sleep 5
      - docker/publish:
          image: node-js-server
          tag: latest
          registry: docker.io
          username: flqvvperso
          password: dckr_pat_zGvXUh5A93gN4RM7K5MAheogFyE

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
